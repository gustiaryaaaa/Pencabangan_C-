#include <iostream>

using namespace std;

int main() {
    int angka;

    cout << "Masukkan suatu angka: ";
    cin >> angka;

    string hasil = (angka % 2 == 0) ? "genap" : "ganjil";

    cout << "Angka " << angka << " merupakan bilangan " << hasil << endl;

    return 0;
}
